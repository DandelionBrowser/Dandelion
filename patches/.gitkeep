# Patches against upstream Chromium files live here, one file per upstream file.
#
# They are generated by `python build/update_patches.py`, never written by hand.
# Every patch is merge debt paid on each Chromium roll, so keep the set small:
# prefer adding code under //dandelion and patching upstream only to call into
# it. See docs/PATCHES.md.
